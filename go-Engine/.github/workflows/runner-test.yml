name: Runner Smoke Test

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: self-hosted
    steps:
      - name: Print system info
        run: |
          echo "Runner is alive"
          hostname
          whoami
          pwd
